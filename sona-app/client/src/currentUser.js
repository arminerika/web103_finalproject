// Temporary placeholder for logged-in user, until real auth (Issue 16) exists.
// id: 1 = testfan (fan) — id: 2 = testadmin (artist, manages Artist 1 only)
// Swap between the two to test both fan and artist views locally.

const currentUser = { id: 1 };

export default currentUser;
